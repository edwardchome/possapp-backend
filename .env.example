# Database Configuration
DB_USER=possapp
DB_PASSWORD=possapp123

# JWT Configuration (generate with: openssl rand -base64 64)
JWT_SECRET=your-base64-encoded-secret-key-here-must-be-at-least-256-bits-long-for-security

# Server Configuration
SERVER_PORT=8080
